document.querySelector('form').addEventListener('submit', function(event) {
  event.preventDefault(); // Previne o envio do formulário para demonstrar a funcionalidade

  alert('Formulário enviado com sucesso!');
});
